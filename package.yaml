name:                hprolog
version:             0.1.0.0
github:              "patrickcsullivan/hprolog"
license:             BSD3
author:              "Patrick Sullivan"
maintainer:          "patrick.sullivan@hey.com"
copyright:           "2022 Patrick Sullivan"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/patrickcsullivan/hprolog#readme>

dependencies:
- base >= 4.7 && < 5
- haskeline
- parsec

library:
  source-dirs: src

executables:
  hprolog-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - hprolog

tests:
  hprolog-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - hprolog
    - hspec
